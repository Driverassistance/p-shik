{
  "name": "pshik-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env dist/server.js",
    "build": "tsup src/server.ts src/migrate.ts src/migrate_cli.ts --format esm --target es2022 --clean",
    "start": "node dist/server.js",
    "migrate": "node dist/migrate_cli.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.16.0",
    "tsup": "^8.2.4",
    "typescript": "^5.6.3"
  }
}